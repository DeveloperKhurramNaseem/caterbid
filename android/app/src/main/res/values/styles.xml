<?xml version="1.0" encoding="utf-8"?>
<resources>
    <string name="google_maps_api_key">AIzaSyDTWHH3UoPee73i413CpT62Gy8gJH8LNaw</string>

    <!-- Launch Theme (shown before Flutter engine starts) -->
    <style name="LaunchTheme" parent="@android:style/Theme.NoTitleBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
        <!-- Remove or keep false - doesn't hurt -->
        <item name="android:windowDisablePreview">true</item>
    </style>

    <!-- Normal Theme (after Flutter starts) -->
    <style name="NormalTheme" parent="@android:style/Theme.NoTitleBar">
        <!-- Can be transparent if needed after load -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>
</resources>
